<div class="card">
  <div class="card-body">
    <h1>{{postagem.titulo}}</h1>
    <hr>
    <small>Publicado: {{postagem.data}}</small>
    <hr>  
    <p class="paragrafos">{{postagem.conteudo}}</p>
    <img src="{{postagem.urlimage}}" alt="" class="img-fluid mb-3">
    <p class="paragrafos">{{postagem.conteudosecundario}}</p>
  </div>
</div>
